# スタエフ配信 紹介LP

stand.fm の配信を紹介するランディングページです。
静的HTML/CSS/JavaScript のみで構成しており、ビルド不要・依存パッケージなしで
Vercel にそのままデプロイできます。

## 構成

ヒーロー → 共感 → 内容 → CTA（stand.fm を聴く）の4セクション。

| ファイル | 役割 |
| --- | --- |
| `index.html` | ページ本体。文言の編集はここだけで完結します |
| `styles.css` | デザイン。冒頭にカラーパレットをまとめてあります |
| `script.js` | スクロール時のフェードイン演出のみ |
| `vercel.json` | Vercel の設定（クリーンURL・セキュリティヘッダー） |

## チャンネル情報

- チャンネル名: あきの整えちゃんねる
- URL: https://stand.fm/channels/6a38f52ef16ae5c2da4b3464

チャンネル正式名の末尾には 👗 が付いていますが、明朝体の落ち着いた見た目に
合わせて本ページでは省いています。表示したい場合は `index.html` の
「あきの整えちゃんねる」3か所に追記してください。

配信頻度（2〜3日にいちど）と配信形式（ライブ中心）は、チャンネルページの
2026年6〜7月の投稿から読み取った内容です。実際と異なる場合は
`index.html` のヒーロー部分を修正してください。

## 画像について

| ファイル | 用途 | 寸法 |
| --- | --- | --- |
| `profile.jpg` | プロフィールセクションの写真 | 560×560 |
| `ogp.jpg` | SNSシェア時のサムネイル | 1200×630 |

どちらも元のイラスト（`~/Desktop/7B39F0CD-...png`, 1254×1254）から
`sips` で書き出しています。

### OGP画像の作り直し方

`ogp.jpg` は HTML をレンダリングして生成しています。作り直す手順は次のとおりです。

```sh
# 1. レイアウトを編集（vw単位で記述されています）
#    scratchpad/ogp.html
# 2. HTMLをPNG化 → 中央を切り出し → 1200x630 に縮小 → JPEG化
qlmanage -t -s 2400 -o . ogp.html
sips -c 1260 2400 ogp.html.png --out crop.png
sips -z 630 1200 crop.png --out ogp-full.png
sips -s format jpeg -s formatOptions 86 ogp-full.png --out ogp.jpg
```

macOS の QuickLook は独自のビューポート幅で描画するため、`ogp.html` の
サイズ指定はすべて `vw` 単位にしてあります。`px` で書くとレイアウトが崩れます。

## カラーパレット

`styles.css` 冒頭の `:root` で定義しています。

| 用途 | 名称 | 値 |
| --- | --- | --- |
| メイン | ペールブルー | `#B8D4E3` |
| サブ | ミスティホワイト | `#F5F7F9` |
| アクセント | ソフトグレー | `#C7CCD1` |
| アクセント | ダスティブルー | `#8FA8B8` |
| アクセント | パールホワイト | `#F4F1EA` |
| CTA・リンク | コーンフラワーブルー | `#7B9ACC` |

### ボタンの色について

コーンフラワーブルー `#7B9ACC` に白文字を載せるとコントラスト比が 2.9:1 となり、
WCAG の基準（4.5:1）を大きく下回って読みにくくなります。
そのためボタンの塗りには、同系統で暗さを足した `--cta-deep` `#4A6DA7`（5.2:1）を
使用しています。罫線や見出しの装飾には `#7B9ACC` をそのまま使っています。

より淡い印象にしたい場合は `styles.css` の `--cta-deep` を `#7B9ACC` に変更できますが、
可読性は下がります。

## ローカルでの確認

`index.html` をブラウザで直接開けば確認できます。
CSSを編集しても見た目が変わらない場合は、ブラウザのキャッシュが原因です
（スーパーリロード: `Cmd + Shift + R`）。

## デプロイ

GitHub の `main` ブランチに push すると Vercel が自動でデプロイします。
